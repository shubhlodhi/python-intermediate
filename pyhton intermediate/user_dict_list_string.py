# user dict is a wrapper around dictionary objects for easier dictionary sub-classing
# userlist is a wrapper around list objects for easier lis sub-classing 
# userstring is a wrapper around string objects for easier string subclassing
